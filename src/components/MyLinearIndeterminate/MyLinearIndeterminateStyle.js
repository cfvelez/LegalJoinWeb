import {makeStyles} from '@material-ui/core'

const MyLinearIndetermintaeStyle = makeStyles((theme) => ({
  progress: {
    width: '100%',
    marginBottom: theme.spacing(1)
  },
}));

export default MyLinearIndetermintaeStyle
