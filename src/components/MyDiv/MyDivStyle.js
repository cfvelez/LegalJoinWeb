import {makeStyles} from '@material-ui/core'

const MyDivStyle = makeStyles({
  visible: {
    display:'block',
    marginBottom: 10
  },
  hidden:{
    display:'none'
  }
})

export default MyDivStyle
